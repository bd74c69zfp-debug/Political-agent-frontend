services:
  - type: web
    name: youtube-automation-dashboard
    env: node
    plan: starter
    buildCommand: npm install
    startCommand: node src/app.js
    envVars:
      - key: PORT
        value: 3000
